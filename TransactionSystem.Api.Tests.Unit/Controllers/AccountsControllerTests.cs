using FluentAssertions;
using Microsoft.AspNetCore.Mvc;
using TransactionSystem.Api.Controllers;

namespace TransactionSystem.Api.Tests.Unit.Controllers
{
    public class AccountsControllerTests
    {
        [Fact]
        public void GetAccountsOk()
        {
            //var controller = new AccountsController();

            //var result = controller.GetAccounts() as OkObjectResult;

            //var accounts = result?.Value as string[];

            //accounts.Should().BeEquivalentTo(new[] { "Account1", "Account2", "Hello World" });
        }
    }
}
